import React from 'react';
import './Spinner.css'; // Certifique-se de que o caminho está correto

const Spinner = () => {
  return (
    <div className="spinner">
      <div className="spinner-icon"></div>
    </div>
  );
};

export default Spinner;